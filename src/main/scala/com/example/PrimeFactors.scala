package com.example

object PrimeFactors {
  def result(real_number: Int): List[Int] = {
    val list = List()
    list
  }

//  def result(number: Int, list: List[Int] = List()): List[Int] = {
//    for(n <- 2 to number if (number % n == 0)) {
//      return result(number / n, list :+ n)
//    }
//    list
//  }
}
